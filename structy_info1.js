/* const depthFirstPrint = (arr, source) => {
console.log(source )
  for (const iterator of array[source]) {
    depthFirstPrint(array, iterator)
  }
}
const array = {
  a:['b','c'],
  b:['d'],
  c:['e'],
  d:['f'],
  e:[],
  f:[]
}
depthFirstPrint(array, 'a') */

                                            // ----------------------------------- //
                                            
/* const breadthFirstPrint = (graph, source) => {
  const queue = [source];
  while (queue.length > 0) {
    const current = queue.shift();
    current
    for ( let iterator of graph[current] ) {
      queue.push(iterator)
    }
  }
}

const graph = {
  a:['b','c'],
  b:['d'],
  c:['e'],
  d:['f'],
  e:[],
  f:[]
}

breadthFirstPrint(graph, 'a') */





